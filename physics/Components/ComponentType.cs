using Physics.Interfaces;

namespace Physics.Components
{
    public enum ComponentType
    {
        Unknown = 0,
        Component = 10,
        Defense = 20,
        Inventory = 30,
        Offense = 40
    }
}